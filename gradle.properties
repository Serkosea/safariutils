org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Pin org.gradle.java.home locally if the shell does not use Java 25. A shared
# machine-specific path would make the project fail elsewhere.
#org.gradle.java.home=/usr/lib/jvm/java-25-openjdk

# Fabric. Version-specific values are loaded from gradle/versions/.
default_minecraft_profile=26.1.2
loader_version=0.19.3
loom_version=1.16-SNAPSHOT

# Mod Properties
release_version=1.1.1
maven_group=dev.serko
archives_base_name=safariutils
